# Resume (React + TypeScript)

This repository contains a converted resume into a small React + TypeScript app using Vite.

Quick start:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```
